name: SmartParking

dependencies:
  - python
  - numpy
  - requests
  - pip
  - pip:
    - tb-mqtt-client
    - tb-rest-client
    - psycopg2
    - django
    - folium
    - matplotlib
    - opencv-python
    - tb_rest_api